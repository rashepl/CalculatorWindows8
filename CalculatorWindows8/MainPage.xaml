<Page
    x:Class="CalculatorWindows8.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CalculatorWindows8"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Loaded="Page_Loaded">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" FlowDirection="RightToLeft">
        <Grid.DataContext>
            <local:MainPageViewModel/>
        </Grid.DataContext>
        <Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="170*"/>
            <RowDefinition Height="613*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="1" BorderBrush="#FF2065DE" BorderThickness="4">
            <StackPanel>
                <TextBlock Text="{Binding EquationDisplayed, Mode=OneWay}" Margin="7" FontSize="30" FlowDirection="LeftToRight" TextAlignment="Right" />
                <TextBlock Text="{Binding SelectedValueDisplayed, Mode=OneWay}" Padding="0" Margin="7" FontSize="40" FlowDirection="LeftToRight" TextAlignment="Right" />
            </StackPanel>
        </Border>
        <UserControl x:Name="_calculatorView" Grid.Row="3" FlowDirection="LeftToRight"/>
    </Grid>
</Page>
